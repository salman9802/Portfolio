import ThemeSelectButton from "./ThemeSelectButton";

type Props = {};

const Navbar = (props: Props) => {
  return (
    <div className="py-4">
      <ThemeSelectButton />
    </div>
  );
};

export default Navbar;
